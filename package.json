{
  "name": "walk-together",
  "private": true,
  "version": "0.1.0",
  "description": "同路行第一期：组队出行闭环",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:infra": "docker compose up -d",
    "dev:api": "pnpm --filter @walk-together/shared-types build && pnpm --filter @walk-together/api start:dev",
    "dev:mobile": "pnpm --filter @walk-together/mobile dev:h5",
    "dev:mp": "pnpm --filter @walk-together/mobile dev:mp-weixin",
    "build:types": "pnpm --filter @walk-together/shared-types build",
    "build:api": "pnpm --filter @walk-together/shared-types build && pnpm --filter @walk-together/api build",
    "build:h5": "pnpm --filter @walk-together/mobile build:h5",
    "build:deploy": "pnpm build:api && pnpm build:h5",
    "build:mp": "pnpm --filter @walk-together/mobile build:mp-weixin"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@swc/core",
      "@nestjs/core",
      "@parcel/watcher",
      "unrs-resolver",
      "core-js",
      "core-js-pure"
    ]
  }
}
